Requirements: 

	1. Login / Registration
	2. Upload Assignments from Trainers
	3. Upload Assignment Solutions from Students
	4. Verify uploaded assignments
	5. Students can browse other’s assignments only after verified by trainer

DATABASE: 

	XorHub

	LoginInfo
	PK Username varchar(10) [min: 4, [a-zA-Z0-9_], max: 10]
		 Password varchar(15) [min: 8, SpecialChar + Alphanumeric, max: 15]
		 Usertype varchar(1)  [(S)tudent, (T)eacher, (A)dmin]
		 Status		boolean			[null for Student]

	UserInfo
	FK Username varchar(10)
		 Name			varchar(20) [min: 3, [a-zA-Z ], max: 20]
	FK BatchId  numeric

	Batch
	PK BatchId numeric
		 Name    varchar(10)

	Assignment
	PK AssignmentId numeric
		 Title				varchar(50)
		 Date 				DateTime
	FK TeacherId    varchar(10)
		 Deadline     DateTime
	FK BatchId      numeric
		 Document     varchar(100)

	Solutions
	PK SolutionId    numeric
	FK AssignmentId  numeric
	FK Username			 varchar(10)
		 Status				 varchar(1) [(A)pprove, (R)eject, (P)ending]
		 UploadedOn    DateTime
		 Document			 varchar(100)
		 Comment  		 varchar(200)


MVC Arch:

	IndexController
		Action: 
			Login
			Register
			Logout

		View:
			Index

	HomeController
		Action:
			Student()
			Teacher()
			Assignment(int AssignmentId)

		View:
			Student
			Teacher


